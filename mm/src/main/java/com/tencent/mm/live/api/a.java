package com.tencent.mm.live.api;

import android.content.Context;

public interface a {
    boolean a(Context context, LiveConfig liveConfig);

    boolean b(Context context, LiveConfig liveConfig);

    boolean c(Context context, LiveConfig liveConfig);

    boolean d(Context context, LiveConfig liveConfig);
}
